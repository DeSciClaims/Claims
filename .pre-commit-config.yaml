repos:
  - repo: local
    hooks:
      - id: ruff-undefined-names
        name: Check Python undefined names
        entry: uv run --frozen --extra dev ruff check
        language: system
        types: [python]
